# Jorge Ferreira Movers — Admin Panel

React + Vite + Tailwind CSS.

## Local Development

1. Copy `.env.example` to `.env`
2. Run `npm install`
3. Run `npm run dev -- --port 5174`

Open [http://localhost:5174](http://localhost:5174).

Login: `ferreiramovers.uk@gmail.com` / `MoversFerreira2025@`
