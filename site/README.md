# Jorge Ferreira Movers — Site

Next.js 16 + TypeScript + Tailwind CSS.

## Local Development

1. Copy `.env.local.example` to `.env.local`
2. Run `npm install`
3. Run `npm run dev`

Open [http://localhost:3000](http://localhost:3000).
